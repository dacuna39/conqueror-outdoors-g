import { css } from 'styled-components';
import styled from 'styled-components';

const iconStyle = css`
  padding: 9px;
  background: #eee;
  border: 1px solid #000;
  color: #000;
`;

const IconStyle = styled.i`${iconStyle}`;

export default IconStyle;